/**
 * TTB API URL — set once when you deploy.
 * Leave empty to use automatic detection:
 *   - On localhost → http://localhost:3001
 *   - Otherwise → same origin (your site must serve the API at /api)
 */
window.TTB_API_URL = 'https://ttb-website-hior.onrender.com';
