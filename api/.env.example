# ============================================
#  TTB API - Environment Variables
# ============================================
#
#  1. Copy this file to .env
#  2. Fill in your values
#  3. Never commit the .env file!
#
# ── MongoDB ──────────────────────────────────
# Get this from MongoDB Atlas:
# Atlas > Database > Connect > Drivers > Connection String
MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@cluster0.xxxxx.mongodb.net/ttb?retryWrites=true&w=majority

# ── Admin Auth ───────────────────────────────
# Pick a strong password - this is what you'll use to log in
ADMIN_PASSWORD=your-strong-password-here

# Secret for signing JWT tokens (use a random string)
# Generate one: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-random-jwt-secret-here

# ── Server ───────────────────────────────────
PORT=3001

# Your GitHub Pages URL (for CORS)
ALLOWED_ORIGIN=https://travelingtastebuds.org
